{
  "name": "xzawed-manager",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.10.4",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.1"
  },
  "pnpm": {
    "overrides": {
      "esbuild": ">=0.28.1",
      "vite": "7.3.5"
    }
  }
}
