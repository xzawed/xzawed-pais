name: xzawed-pais

services:
  # ── Infrastructure ──────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: xzawed
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: xzawed_orchestrator
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U xzawed"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  # ── Orchestrator (port 3000) ────────────────────────────────────────────────
  orchestrator:
    build:
      context: .
      dockerfile: xzawedOrchestrator/Dockerfile
    ports:
      - "3000:3000"
    env_file: ./xzawedOrchestrator/.env
    environment:
      REDIS_URL: redis://redis:6379
      PORT: "3000"
      CLAUDE_MODE: ${CLAUDE_MODE:-api}
      MANAGER_URL: http://manager:3001
    volumes:
      - workspace:/workspace
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
      manager:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "3"
    restart: unless-stopped

  # ── Manager (port 3001) ─────────────────────────────────────────────────────
  manager:
    build:
      context: .
      dockerfile: xzawedManager/Dockerfile
    ports:
      - "3001:3001"
    env_file: ./xzawedManager/.env
    environment:
      REDIS_URL: redis://redis:6379
      PORT: "3001"
      CLAUDE_MODE: ${CLAUDE_MODE:-api}
      # 세션·도메인 지식(위키) 영속화 — postgres 공유 DB(테이블명 충돌 없음). 미설정 시 인메모리.
      DATABASE_URL: postgres://xzawed:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@postgres:5432/xzawed_orchestrator
    volumes:
      - workspace:/workspace
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3001/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "3"
    restart: unless-stopped

  # ── Planner (port 3002) ─────────────────────────────────────────────────────
  planner:
    build:
      context: .
      dockerfile: xzawedPlanner/Dockerfile
    ports:
      - "3002:3002"
    env_file: ./xzawedPlanner/.env
    environment:
      REDIS_URL: redis://redis:6379
      PORT: "3002"
      WORKSPACE_ROOT: /workspace
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3002/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "3"
    restart: unless-stopped

  # ── Developer (port 3003) ───────────────────────────────────────────────────
  developer:
    build:
      context: .
      dockerfile: xzawedDeveloper/Dockerfile
    ports:
      - "3003:3003"
    env_file: ./xzawedDeveloper/.env
    environment:
      REDIS_URL: redis://redis:6379
      PORT: "3003"
      WORKSPACE_ROOT: /workspace
    volumes:
      - workspace:/workspace
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3003/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "3"
    restart: unless-stopped

  # ── Designer (port 3004) ────────────────────────────────────────────────────
  designer:
    build:
      context: .
      dockerfile: xzawedDesigner/Dockerfile
    ports:
      - "3004:3004"
    env_file: ./xzawedDesigner/.env
    environment:
      REDIS_URL: redis://redis:6379
      PORT: "3004"
      WORKSPACE_ROOT: /workspace
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3004/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "3"
    restart: unless-stopped

  # ── Tester (port 3005) ──────────────────────────────────────────────────────
  tester:
    build:
      context: .
      dockerfile: xzawedTester/Dockerfile
    ports:
      - "3005:3005"
    env_file: ./xzawedTester/.env
    environment:
      REDIS_URL: redis://redis:6379
      PORT: "3005"
      WORKSPACE_ROOT: /workspace
    volumes:
      - workspace:/workspace
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3005/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "3"
    restart: unless-stopped

  # ── Builder (port 3006) ─────────────────────────────────────────────────────
  builder:
    build:
      context: .
      dockerfile: xzawedBuilder/Dockerfile
    ports:
      - "3006:3006"
    env_file: ./xzawedBuilder/.env
    environment:
      REDIS_URL: redis://redis:6379
      PORT: "3006"
      WORKSPACE_ROOT: /workspace
    volumes:
      - workspace:/workspace
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3006/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "3"
    restart: unless-stopped

  # ── Watcher (port 3007) ─────────────────────────────────────────────────────
  watcher:
    build:
      context: .
      dockerfile: xzawedWatcher/Dockerfile
    ports:
      - "3007:3007"
    env_file: ./xzawedWatcher/.env
    environment:
      REDIS_URL: redis://redis:6379
      PORT: "3007"
      WORKSPACE_ROOT: /workspace
    volumes:
      - workspace:/workspace
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3007/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "3"
    restart: unless-stopped

  # ── Security (port 3008) ────────────────────────────────────────────────────
  security:
    build:
      context: .
      dockerfile: xzawedSecurity/Dockerfile
    ports:
      - "3008:3008"
    env_file: ./xzawedSecurity/.env
    environment:
      REDIS_URL: redis://redis:6379
      PORT: "3008"
      WORKSPACE_ROOT: /workspace
    volumes:
      - workspace:/workspace:ro
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3008/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "3"
    restart: unless-stopped

volumes:
  redis-data:
  postgres-data:
  workspace:
